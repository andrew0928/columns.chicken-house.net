# Draft Mode Configuration
# 專為快速預覽設計，只處理最新文章
# 使用方式: jekyll serve --config _config.yml,_config.draft.yml

# 限制只處理最新 5 篇文章
limit_posts: 20

# 排除大量靜態內容
exclude:
  - _synthesis

# 加速建置
# incremental: true
profile: true
